const utilisation = [{ name: "Ambulances", value: 83 }, { name: "Rescue Teams", value: 76 }, { name: "Medical Teams", value: 88 }, { name: "Water", value: 71 }, { name: "Food", value: 69 }, { name: "Shelters", value: 81 }];

function ResourceUtilisation() { return <section className="rounded-md border border-slate-800 bg-[#111b24] p-4"><p className="text-[10px] font-bold tracking-[0.16em] text-cyan-400">RESOURCE UTILISATION</p><h2 className="mt-1 text-sm font-semibold text-slate-100">Operational load by category</h2><div className="mt-4 space-y-3">{utilisation.map((item) => { const status = item.value >= 86 ? "Critical Load" : item.value >= 80 ? "High Load" : "Healthy"; const color = item.value >= 86 ? "bg-red-400" : item.value >= 80 ? "bg-orange-400" : "bg-emerald-400"; return <div key={item.name}><div className="flex justify-between text-[10px]"><span className="text-slate-400">{item.name}</span><span className="text-slate-300">{item.value}% <b className={`ml-2 font-medium ${item.value >= 86 ? "text-red-300" : item.value >= 80 ? "text-orange-300" : "text-emerald-300"}`}>{status}</b></span></div><div className="mt-1.5 h-1.5 rounded-full bg-slate-800"><div className={`h-full rounded-full ${color}`} style={{ width: `${item.value}%` }} /></div></div>; })}</div></section>; }

export default ResourceUtilisation;