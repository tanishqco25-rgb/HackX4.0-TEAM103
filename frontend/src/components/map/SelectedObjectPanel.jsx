import { LocateFixed, Route, UserPlus, X } from "lucide-react";

function SelectedObjectPanel({ selection, onClose, onApprove, onAction }) { if (!selection) return null; const { item, kind } = selection; return <aside className="absolute bottom-3 right-3 z-1000 w-72 rounded border border-slate-700/90 bg-[#0d151c]/95 p-4 shadow-xl"><div className="flex items-start justify-between"><div><p className="text-[9px] font-bold tracking-[0.16em] text-cyan-400">SELECTED {kind.toUpperCase()}</p><h2 className="mt-1 text-sm font-semibold text-slate-100">{item.id || item.name}</h2></div><button type="button" aria-label="Close selected object" onClick={onClose} className="text-slate-500 hover:text-slate-100"><X size={15} /></button></div>{kind === "incident" && <div className="mt-3 space-y-2 text-[11px] text-slate-400"><p>{item.zone} · {item.type}</p><p className="font-semibold text-red-300">{item.severity} · {item.affected.toLocaleString()} affected</p><p>Resource coverage: <b className="text-orange-300">{item.coverage}%</b></p><p>Required: {item.required.join(" + ")}</p></div>}{kind === "resource" && <div className="mt-3 space-y-2 text-[11px] text-slate-400"><p>{item.type} · {item.status}</p><p>Location: <b className="text-slate-200">{item.location}</b></p><p>Assigned: {item.zone}</p><p>ETA to {item.zone}: <b className="text-cyan-300">{item.eta}</b></p></div>}{kind === "hospital" && <div className="mt-3 text-[11px] text-slate-400"><p>Capacity: {item.capacity}</p><p>Occupied: {item.occupied}</p><p>Available: <b className="text-emerald-300">{item.available}</b></p><p>Status: {item.status}</p></div>}{kind === "shelter" && <div className="mt-3 text-[11px] text-slate-400"><p>Capacity: {item.capacity}</p><p>Occupied: {item.occupied}</p><p>Available: <b className="text-emerald-300">{item.available}</b></p><p>Status: {item.status}</p></div>}{kind === "relief" && <div className="mt-3 text-[11px] text-slate-400"><p>Water: {item.water}</p><p>Food: {item.food}</p><p>Medicine: {item.medicine}</p><p>Status: {item.status}</p></div>}<div className="mt-4 flex flex-wrap gap-1.5">{kind === "resource" && <><button type="button" onClick={() => onAction("assign")} className="flex items-center gap-1 rounded bg-cyan-400 px-2 py-1.5 text-[9px] font-bold text-slate-950"><UserPlus size={11} /> ASSIGN</button><button type="button" onClick={() => onAction("route")} className="flex items-center gap-1 rounded border border-slate-700 px-2 py-1.5 text-[9px] font-bold text-slate-300"><Route size={11} /> ROUTE</button></>}{kind === "incident" && <button type="button" onClick={() => onAction("allocate")} className="flex items-center gap-1 rounded bg-cyan-400 px-2 py-1.5 text-[9px] font-bold text-slate-950"><UserPlus size={11} /> ALLOCATE</button>}{kind === "route" && item.status === "AI Recommended" && <button type="button" onClick={onApprove} className="rounded bg-emerald-400 px-2 py-1.5 text-[9px] font-bold text-slate-950">APPROVE ALLOCATION</button>}<button type="button" onClick={() => onAction("focus")} className="flex items-center gap-1 rounded border border-slate-700 px-2 py-1.5 text-[9px] font-bold text-slate-300"><LocateFixed size={11} /> FOCUS</button></div></aside>; }
export default SelectedObjectPanel;