import { Filter } from "lucide-react";

function MapPageFilters({ filters, onChange }) { const select = (label, key, options) => <label className="block"><span className="mb-1 block text-[9px] font-bold tracking-widest text-slate-500">{label}</span><select aria-label={label} value={filters[key]} onChange={(event) => onChange(key, event.target.value)} className="h-8 w-full rounded border border-slate-700 bg-[#111b24] px-2 text-[10px] text-slate-300 outline-none"><option>All</option>{options.map((option) => <option key={option}>{option}</option>)}</select></label>; return <div className="absolute left-3 top-3 z-1000 w-48 rounded border border-slate-700/90 bg-[#0d151c]/95 p-3 shadow-xl"><div className="mb-3 flex items-center gap-2 text-[10px] font-bold tracking-[0.16em] text-slate-200"><Filter size={14} className="text-cyan-300" />MAP FILTERS</div>{select("Incident Severity", "severity", ["Critical", "High", "Medium", "Low"])}<div className="mt-2">{select("Resource Type", "resourceType", ["Ambulance", "Rescue Team", "Medical Team", "Supply Vehicle"])}</div><div className="mt-2">{select("Resource Status", "resourceStatus", ["Available", "Deployed", "En Route"])}</div></div>; }
export default MapPageFilters;