import { Search } from "lucide-react";

function MapSearch({ query, onQueryChange, results, onSelect }) { return <div className="absolute left-3 top-3 z-1100 mt-63.75 w-64"><label className="relative block"><Search size={15} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-500" /><input aria-label="Search map objects" value={query} onChange={(event) => onQueryChange(event.target.value)} placeholder="Search incident, resource or zone..." className="h-9 w-full rounded border border-slate-700/90 bg-[#0d151c]/95 pl-9 pr-3 text-[10px] text-slate-200 shadow-xl outline-none placeholder:text-slate-600 focus:border-cyan-400/60" /></label>{query && <div className="mt-1 max-h-40 overflow-auto rounded border border-slate-700 bg-[#0d151c] shadow-xl">{results.map(({ item, kind, label }) => <button type="button" key={`${kind}-${label}`} onClick={() => onSelect(item, kind)} className="block w-full px-3 py-2 text-left text-[10px] text-slate-300 hover:bg-slate-800">{label}<span className="ml-2 text-slate-600">{kind}</span></button>)}</div>}</div>; }
export default MapSearch;