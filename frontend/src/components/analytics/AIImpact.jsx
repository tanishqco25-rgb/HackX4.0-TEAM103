import { ArrowDownRight, ArrowUpRight, BrainCircuit } from "lucide-react";

function AIImpact({ impact }) { const metrics = [["Average Response", `${impact.without.response} min`, `${impact.with.response} min`, true], ["Demand Coverage", `${impact.without.coverage}%`, `${impact.with.coverage}%`, false], ["Critical Gaps", impact.without.gaps, impact.with.gaps, true], ["Unserved Population", impact.without.unserved.toLocaleString(), impact.with.unserved.toLocaleString(), true]]; return <section className="rounded-md border border-cyan-400/20 bg-[#111b24] p-4"><div className="flex items-center gap-2"><BrainCircuit size={17} className="text-cyan-300" /><div><p className="text-[10px] font-bold tracking-[0.16em] text-cyan-400">AI ALLOCATION IMPACT</p><h2 className="mt-1 text-sm font-semibold text-slate-100">Manual response versus ResQGrid AI</h2></div></div><div className="mt-4 grid grid-cols-2 gap-3">{metrics.map(([label, before, after, down]) => <div key={label} className="rounded border border-slate-800 bg-slate-900/35 p-3"><p className="text-[10px] text-slate-500">{label}</p><div className="mt-2 flex items-center justify-between text-xs"><span className="text-slate-500">Without AI <b className="ml-1 text-slate-300">{before}</b></span><span className="text-emerald-300">With AI <b className="ml-1">{after}</b></span></div><p className="mt-2 flex items-center gap-1 text-[10px] text-emerald-300">{down ? <ArrowDownRight size={12} /> : <ArrowUpRight size={12} />}Simulated improvement</p></div>)}</div></section>; }

export default AIImpact;