import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts";

function IncidentPerformance({ data }) { const total = data.reduce((sum, item) => sum + item.value, 0); return <section className="rounded-md border border-slate-800 bg-[#111b24] p-4"><p className="text-[10px] font-bold tracking-[0.16em] text-red-300">INCIDENT RESPONSE PERFORMANCE</p><h2 className="mt-1 text-sm font-semibold text-slate-100">Resolution status</h2><div className="mt-3 flex items-center gap-4"><div className="h-36 w-36"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={43} outerRadius={62} paddingAngle={3}>{data.map((item) => <Cell key={item.name} fill={item.color} />)}</Pie><Tooltip contentStyle={{ background: "#0d151c", border: "1px solid #334155", borderRadius: 4, fontSize: 11 }} /></PieChart></ResponsiveContainer></div><div className="grid flex-1 grid-cols-2 gap-3 text-[11px]"><span className="text-slate-500">Total incidents<b className="mt-1 block text-lg text-slate-100">{total}</b></span>{data.map((item) => <span key={item.name} className="text-slate-500"><i className="mr-1 inline-block h-2 w-2 rounded-full" style={{ background: item.color }} />{item.name}<b className="ml-1 text-slate-300">{item.value}</b></span>)}<span className="text-slate-500">Avg resolution<b className="mt-1 block text-slate-200">42 min</b></span></div></div></section>; }

export default IncidentPerformance;