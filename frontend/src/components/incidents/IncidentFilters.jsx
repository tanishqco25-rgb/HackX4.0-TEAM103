import { Filter, RefreshCw, Search, X } from "lucide-react";
import { incidentSeverities, incidentStatuses, incidentTypes } from "../../data/incidents";

function IncidentFilters({ query, onQueryChange, severity, type, status, onFilterChange, onClear, onRefresh, refreshedAt, onAdd }) {
  const hasFilters = query || severity !== "All" || type !== "All" || status !== "All";
  return <section className="rounded-md border border-slate-800 bg-[#111b24] p-4"><div className="flex flex-wrap items-center gap-2"><label className="relative min-w-55 flex-1"><Search size={16} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => onQueryChange(event.target.value)} placeholder="Search ID, location or type" aria-label="Search incidents" className="h-9 w-full rounded border border-slate-700 bg-[#0d151c] pl-9 pr-3 text-xs text-slate-200 outline-none placeholder:text-slate-600 focus:border-cyan-400/60" /></label><div className="flex items-center gap-2 text-slate-500"><Filter size={15} /><span className="hidden text-[10px] font-bold tracking-[0.12em] sm:inline">FILTERS</span></div><select value={severity} onChange={(event) => onFilterChange("severity", event.target.value)} aria-label="Filter by severity" className="h-9 rounded border border-slate-700 bg-[#0d151c] px-2 text-xs text-slate-300 outline-none focus:border-cyan-400/60"><option>All</option>{incidentSeverities.map((item) => <option key={item}>{item}</option>)}</select><select value={type} onChange={(event) => onFilterChange("type", event.target.value)} aria-label="Filter by disaster type" className="h-9 max-w-38.75 rounded border border-slate-700 bg-[#0d151c] px-2 text-xs text-slate-300 outline-none focus:border-cyan-400/60"><option>All</option>{incidentTypes.map((item) => <option key={item}>{item}</option>)}</select><select value={status} onChange={(event) => onFilterChange("status", event.target.value)} aria-label="Filter by status" className="h-9 rounded border border-slate-700 bg-[#0d151c] px-2 text-xs text-slate-300 outline-none focus:border-cyan-400/60"><option>All</option>{incidentStatuses.map((item) => <option key={item}>{item}</option>)}</select>{hasFilters && <button type="button" onClick={onClear} className="flex h-9 items-center gap-1 rounded px-2 text-[10px] font-bold text-slate-500 hover:bg-slate-800 hover:text-slate-200"><X size={13} /> CLEAR</button>}<button type="button" onClick={onRefresh} className="flex h-9 items-center gap-2 rounded border border-slate-700 px-3 text-xs text-slate-400 hover:bg-slate-800 hover:text-slate-100"><RefreshCw size={14} /> <span className="hidden sm:inline">{refreshedAt ? "UPDATED" : "REFRESH"}</span></button><button type="button" onClick={onAdd} className="h-9 rounded bg-cyan-400 px-3 text-xs font-bold text-slate-950 hover:bg-cyan-300">+ ADD INCIDENT</button></div></section>;
}

export default IncidentFilters;