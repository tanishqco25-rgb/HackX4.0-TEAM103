function AllocationMatrix({ rows, highlight }) {
  const resources = ["AMB-A12", "AMB-A18", "RES-R04", "MED-M02"];
  const tone = (score) => score >= 90 ? "bg-emerald-400/25 text-emerald-200" : score >= 75 ? "bg-cyan-400/15 text-cyan-200" : score >= 60 ? "bg-orange-400/10 text-orange-200" : "bg-slate-800 text-slate-400";
  return <section className="rounded-md border border-slate-800 bg-[#111b24] p-4"><div className="flex flex-wrap items-end justify-between gap-2"><div><p className="text-[10px] font-bold tracking-[0.16em] text-slate-500">ALLOCATION MATRIX</p><h2 className="mt-1 text-sm font-semibold text-slate-100">Incident-to-resource match score</h2></div><div className="flex gap-3 text-[9px] text-slate-500"><span><i className="mr-1 inline-block h-2 w-2 bg-slate-800" />LOW</span><span><i className="mr-1 inline-block h-2 w-2 bg-cyan-400/40" />MEDIUM</span><span><i className="mr-1 inline-block h-2 w-2 bg-emerald-400/50" />HIGH</span></div></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-120 border-separate border-spacing-1 text-center text-[10px]"><thead><tr><th className="text-left text-slate-500">INCIDENT</th>{resources.map((resource) => <th key={resource} className="pb-1 font-bold text-slate-500">{resource}</th>)}</tr></thead><tbody>{rows.map((row) => <tr key={row.incidentId}><td className="py-2 text-left font-semibold text-slate-300">{row.incidentId}</td>{resources.map((resource) => <td key={resource} className={`rounded px-2 py-2 font-semibold ${tone(row.scores[resource])} ${highlight?.incidentId === row.incidentId && highlight?.resourceId === resource ? "ring-1 ring-white" : ""}`}>{row.scores[resource]}</td>)}</tr>)}</tbody></table></div></section>;
}

export default AllocationMatrix;