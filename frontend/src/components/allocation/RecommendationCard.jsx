import { Check, Eye, X, Zap } from "lucide-react";

function RecommendationCard({ recommendation, incident, resource, status, onApprove, onReject, onReasoning }) {
  return <article className={`rounded-md border bg-[#111b24] p-4 ${status === "Approved" ? "border-emerald-400/35" : status === "Rejected" ? "border-slate-700 opacity-65" : "border-slate-800"}`}><div className="flex items-start justify-between"><div><p className="text-[9px] font-bold tracking-[0.18em] text-cyan-400">RECOMMENDATION #{recommendation.id}</p><h3 className="mt-2 text-sm font-semibold text-slate-100">{resource.id} <span className="font-normal text-slate-500">→ {incident.zone}</span></h3></div>{status ? <span className={`rounded border px-2 py-1 text-[9px] font-bold ${status === "Approved" ? "border-emerald-400/25 bg-emerald-400/10 text-emerald-300" : "border-slate-700 text-slate-500"}`}>{status.toUpperCase()}</span> : <Zap size={16} className="text-amber-300" />}</div><div className="mt-4 grid grid-cols-4 gap-2 border-y border-slate-800/80 py-3 text-[10px]"><span className="text-slate-500">FROM<b className="mt-1 block text-slate-300">{resource.location}</b></span><span className="text-slate-500">INCIDENT<b className="mt-1 block text-slate-300">{incident.id}</b></span><span className="text-slate-500">PRIORITY<b className="mt-1 block text-orange-300">{recommendation.priority}/100</b></span><span className="text-slate-500">ETA<b className="mt-1 block text-cyan-300">{recommendation.eta}</b></span></div><p className="mt-3 text-[11px] leading-5 text-slate-400"><span className="font-semibold text-slate-300">Reason:</span> {recommendation.reason}</p><div className="mt-3 flex gap-4 text-[10px]"><span className="text-slate-500">Demand coverage <b className="text-emerald-300">{recommendation.impactCoverage}</b></span><span className="text-slate-500">Response time <b className="text-emerald-300">{recommendation.impactTime}</b></span><span className="text-slate-500">Confidence <b className="text-cyan-300">{recommendation.confidence}%</b></span></div><div className="mt-4 flex flex-wrap gap-2"><button type="button" disabled={Boolean(status)} onClick={onApprove} className="flex items-center gap-1.5 rounded border border-emerald-400/25 bg-emerald-400/10 px-2.5 py-2 text-[10px] font-bold text-emerald-300 disabled:cursor-not-allowed disabled:opacity-70"><Check size={13} /> APPROVE ALLOCATION</button><button type="button" onClick={onReasoning} className="flex items-center gap-1.5 rounded border border-slate-700 px-2.5 py-2 text-[10px] font-bold text-slate-400 hover:text-slate-100"><Eye size={13} /> VIEW REASONING</button><button type="button" disabled={Boolean(status)} onClick={onReject} className="flex items-center gap-1.5 rounded border border-slate-700 px-2.5 py-2 text-[10px] font-bold text-slate-500 hover:text-red-300 disabled:cursor-not-allowed"><X size={13} /> REJECT</button></div></article>;
}

export default RecommendationCard;