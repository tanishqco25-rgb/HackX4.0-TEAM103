import { CheckCircle2, Filter, MapPin } from "lucide-react";

const types = ["All", "Ambulance", "Rescue Team", "Medical Team", "Water Supply", "Food Supply", "Medicine Kit"];

function AvailableResources({ resources, selectedResource, onSelect, filter, onFilterChange }) {
  return <section className="rounded-md border border-slate-800 bg-[#111b24] p-4"><div className="flex items-start justify-between gap-2"><div><p className="text-[10px] font-bold tracking-[0.16em] text-blue-300">02 / AVAILABLE RESOURCES</p><h2 className="mt-1 text-sm font-semibold text-slate-100">Ready for allocation</h2></div><Filter size={16} className="text-slate-500" /></div><select aria-label="Filter allocation resources" value={filter} onChange={(event) => onFilterChange(event.target.value)} className="mt-3 h-8 w-full rounded border border-slate-700 bg-[#0d151c] px-2 text-[11px] text-slate-300 outline-none"><option>All</option>{types.slice(1).map((type) => <option key={type}>{type}</option>)}</select><div className="mt-3 space-y-2">{resources.map((resource) => <button type="button" key={resource.id} onClick={() => onSelect(resource)} className={`w-full rounded border p-3 text-left ${selectedResource?.id === resource.id ? "border-cyan-400/40 bg-cyan-400/6" : "border-slate-800 hover:border-slate-700 hover:bg-slate-800/30"}`}><div className="flex items-start justify-between"><div><p className="text-xs font-semibold text-slate-100">{resource.id}</p><p className="mt-1 text-[11px] text-slate-400">{resource.type}</p></div><span className={`text-[9px] font-bold ${resource.availability === "Available" ? "text-emerald-400" : "text-orange-300"}`}><CheckCircle2 size={15} className="inline mr-1" />{resource.availability}</span></div>{resource.assignedIncident && <p className="mt-2 text-[10px] text-orange-300">Assigned to {resource.assignedIncident}</p>}<div className="mt-3 grid grid-cols-3 gap-2 text-[10px]"><span className="flex items-center gap-1 text-slate-500"><MapPin size={11} />{resource.location}</span><span className="text-slate-400">{resource.distance}</span><span className="text-right font-medium text-cyan-300">{resource.travel}</span></div></button>)}{resources.length === 0 && <p className="py-8 text-center text-xs text-slate-500">No available resources match this type.</p>}</div></section>;
}

export default AvailableResources;