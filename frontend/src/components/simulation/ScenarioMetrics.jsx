import { ArrowDownRight, ArrowUpRight, CircleAlert, Clock3, ShieldAlert, Users } from "lucide-react";

function ScenarioMetrics({ current, simulated }) {
  const metrics = [["Active Incidents", current.incidents, simulated.incidents, Users, false], ["Affected Population", current.population.toLocaleString(), simulated.population.toLocaleString(), Users, false], ["Demand Coverage", `${current.coverage}%`, `${simulated.coverage}%`, ShieldAlert, true], ["Average Response", `${current.response} min`, `${simulated.response} min`, Clock3, true], ["Critical Gaps", current.gaps, simulated.gaps, CircleAlert, true]];
  return <section className="grid gap-3 xl:grid-cols-5">{metrics.map(([label, base, next, Icon, inverse]) => { const numericBase = parseFloat(base.toString().replace(/,/g, "")); const numericNext = parseFloat(next.toString().replace(/,/g, "")); const improved = inverse ? numericNext < numericBase : numericNext === numericBase; return <article key={label} className="rounded-md border border-slate-800 bg-[#111b24] p-3"><div className="flex justify-between"><p className="text-[10px] font-bold tracking-[0.12em] text-slate-500">{label}</p><Icon size={15} className="text-slate-500" /></div><div className="mt-3 grid grid-cols-2 gap-2"><div><p className="text-[9px] text-slate-600">CURRENT</p><p className="mt-1 text-sm text-slate-400">{base}</p></div><div><p className="text-[9px] text-slate-600">SIMULATED</p><p className={`mt-1 text-sm font-semibold ${improved ? "text-emerald-300" : "text-orange-300"}`}>{next}</p></div></div><div className={`mt-2 flex items-center gap-1 text-[10px] ${improved ? "text-emerald-300" : "text-orange-300"}`}>{improved ? <ArrowDownRight size={12} /> : <ArrowUpRight size={12} />}{improved ? "Improved" : "Higher demand"}</div></article>; })}</section>;
}

export default ScenarioMetrics;