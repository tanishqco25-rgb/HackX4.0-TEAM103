import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

function ScenarioComparison({ current, simulated }) {
  const data = [{ name: "Response min", baseline: current.response, simulated: simulated.response }, { name: "Coverage %", baseline: current.coverage, simulated: simulated.coverage }, { name: "Critical gaps", baseline: current.gaps, simulated: simulated.gaps }, { name: "Utilisation %", baseline: current.utilisation, simulated: simulated.utilisation }];
  return <section className="rounded-md border border-slate-800 bg-[#111b24] p-4"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold tracking-[0.16em] text-slate-500">SCENARIO COMPARISON</p><h2 className="mt-1 text-sm font-semibold text-slate-100">Baseline versus simulated</h2></div><div className="flex gap-3 text-[10px] text-slate-500"><span><i className="mr-1 inline-block h-2 w-2 bg-slate-600" />Baseline</span><span><i className="mr-1 inline-block h-2 w-2 bg-orange-400" />Simulated</span></div></div><div className="mt-3 h-56"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={{ top: 4, right: 4, left: -20, bottom: 0 }}><CartesianGrid stroke="#24323d" vertical={false} /><XAxis dataKey="name" tick={{ fill: "#64748b", fontSize: 9 }} axisLine={false} tickLine={false} /><YAxis tick={{ fill: "#64748b", fontSize: 9 }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ background: "#0d151c", border: "1px solid #334155", borderRadius: 4, fontSize: 11 }} /><Bar dataKey="baseline" fill="#475569" radius={[2, 2, 0, 0]} /><Bar dataKey="simulated" fill="#fb923c" radius={[2, 2, 0, 0]} /></BarChart></ResponsiveContainer></div><div className="grid grid-cols-3 gap-2 border-t border-slate-800 pt-3 text-[10px]"><span className="text-slate-500">Response<b className="mt-1 block text-slate-200">{current.response} → {simulated.response} min</b></span><span className="text-slate-500">Coverage<b className="mt-1 block text-cyan-300">{current.coverage} → {simulated.coverage}%</b></span><span className="text-slate-500">Unserved<b className="mt-1 block text-orange-300">{current.unserved.toLocaleString()} → {simulated.unserved.toLocaleString()}</b></span></div></section>;
}

export default ScenarioComparison;